1. Все еще нет .gitignore, Makefile и .golangci.yaml
2. Каждый твой коммит по возможности должен содержать краткую информацию, что было пофикшено
3. что за файл netlinkprocess?
4. Ужасный нейминг файлов и пакетов! По возможности пакеты и файлы нужно называть коротко но что бы отражали суть. 
    Если не получается написать название в одно слово, то сложносоставные имена пишутся через '-'.
    Вот этот файл netlinkprocessnew.go нужно переименовать! Вот у тебя в этом файле описан коллектор, 
    вот так и нужно назвать - collector.go, а пакет назвать просто netlink + в этот пакет перенести decode.go и убрать netlinkdecode
5. Файлы console_printer.go и printer_interface.go переименовать! 
    В названиеях нижнее подчеркивание допускается только у файлов с тестами.
    В данном случае пакет можно переименовать из printtcpudp в просто printer,
    соответственно папку назвать так же printer. В этой папке два файла: printer.go и interface.go относящиеся к пакету printer

